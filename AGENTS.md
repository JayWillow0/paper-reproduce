# AGENTS.md

## 仓库定位

本仓库集中保存论文复现项目。每篇论文使用一个独立一级目录，目录名采用简短、稳定、可识别的英文名称。

## 目录约定

- 每个论文目录必须包含 `README.md`、`requirements.txt`、`experiments/`、`REPRODUCIBILITY.md` 和 `input_data.sha256`。
- 论文原仓库统一放在各项目的 `repo/` 下，默认不进入 Git。
- 实验源码按 `experiments/exp_<letter>/` 分组，代码、同次运行日志、指标和诊断图放在同一实验目录。
- 运行环境、命令、结果哈希和结论边界放在 `REPRODUCIBILITY.md`，固定输入哈希放在 `input_data.sha256`。
- 只保留当前协议的日志和指标。被替代的产物在确认新产物完整后删除。

## 工程规则

- 所有结论从独立样本、估计对象和评估协议出发，不能用扩增切片数替代独立实验数。
- 修改实验协议时先更新项目 `AGENTS.md` 和 README，再修改代码。
- 随机实验必须固定 seed，并记录数据划分、环境、输入和输出哈希。
- 不提交虚拟环境、缓存、系统文件、上游仓库副本或临时权重。
- 不得通过注释错误或跳过失败检查来伪造可运行状态。
- 删除文件、改写 Git 历史、推送或公开发布前必须获得用户明确许可。

## 许可与引用

- LiuYang 原创的复现框架、验证工具、实验增量代码和说明文档采用根目录 MIT License。
- 论文、官方代码、原始数据、论文图片、第三方权重和衍生材料不因进入本仓库而自动采用 MIT。
- 每个论文项目必须登记论文 DOI、官方仓库、固定 commit、上游许可状态和引用方式。
- 搬运或改写上游代码时必须标明来源。上游无 LICENSE 时，不得把上游代码宣称为本项目原创 MIT 代码。
- 公开发布前核对 `THIRD_PARTY_NOTICE.md`，确认 Git 暂存区没有误收上游仓库、原始数据或本地权重。

## 验证要求

- Python 文件至少通过 `python -m py_compile`。
- 已保存结果必须通过项目提供的完整性检查。
- 长时训练只在用户明确授权后运行。
